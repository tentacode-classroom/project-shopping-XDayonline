{% extends 'base.html.twig' %}

{% block title %}Chicken{% endblock %}

{% block body %}
    <h1>Chicken</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ chicken.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ chicken.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ chicken.description }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ chicken.price }}</td>
            </tr>
            <tr>
                <th>Urlimg</th>
                <td>{{ chicken.urlimg }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('chicken_index') }}">back to list</a>

    <a href="{{ path('chicken_edit', {'id': chicken.id}) }}">edit</a>

    {{ include('chicken/_delete_form.html.twig') }}
{% endblock %}
